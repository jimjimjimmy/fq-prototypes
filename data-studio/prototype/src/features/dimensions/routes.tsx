import { Route } from 'react-router-dom';
import { DimensionsPage } from './DimensionsPage';

export const dimensionsRoutes = (
  <Route key="dimensions" path="dimensions" element={<DimensionsPage />} />
);
